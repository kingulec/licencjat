"""
This is setup file for market basket analysis project

requirements.txt is a file including the required packages

* in order to install your package run the fallowing
from inside the market_basket_analysis directory
    'pip install -e .'
* in order to install just required packages run
    'pip install -r "requirement,txt"'

"""
from setuptools import setup, find_packages

setup(
    name='',
    packages=find_packages(),
    install_requires=[]
)